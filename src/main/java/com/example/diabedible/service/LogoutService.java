package com.example.diabedible.service;

import com.example.diabedible.utils.ViewManager;

/**
 * Handles application logout flow.
 */
public interface LogoutService {
    void logout(ViewManager viewManager);
}
